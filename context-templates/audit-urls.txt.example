# Onsite audit URL list (System 3).
# One URL per line. Lines starting with # are ignored.
# Recommended: homepage + 2 most-important pages (top revenue / top traffic).
# More URLs = higher cost (~$0.05 per URL via DataForSEO Lighthouse).
https://www.your-site.com/
https://www.your-site.com/features/ai-visibility/
https://www.your-site.com/blog/query-fan-out-ai-search/
